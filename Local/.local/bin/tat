#!/usr/bin/env bash

######################################################################
#                  Pipe `tmux ls` to fzf and attach                  #
######################################################################
for s in $(tmux ls | awk '{print $1}' | rg ':' -r '' | fzf --header="Attach Sessions >"); do
    tmux attach-session -t "$s"
done
